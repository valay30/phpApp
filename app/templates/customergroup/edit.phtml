<?php include 'app/templates/menu.phtml'; ?>

<h2><?php echo $data->customer_group_id ? 'Edit Customer Group' : 'Add New Customer Group'; ?></h2>

<form method="POST" action="index.php?c=customergroup&a=save">
    <input type="hidden" name="customergroup[customer_group_id]" value="<?php echo $data->customer_group_id; ?>">

    <table border="1" width="100%">
        <tr>
            <td>Name</td>
            <td><input type="text" name="customergroup[group_name]" value="<?php echo $data->group_name; ?>"></td>
        </tr>
        <tr>
            <td>Description</td>
            <td><input type="text" name="customergroup[description]" value="<?php echo $data->description; ?>"></td>
        </tr>
        <tr>
            <td>Status</td>
            <td>
                <select name="customergroup[status]">
                    <option value="1" <?php echo ($data->status == 1) ? 'selected' : ''; ?>>Enabled</option>
                    <option value="0" <?php echo ($data->status == 0) ? 'selected' : ''; ?>>Disabled</option>
                </select>
            </td>
        </tr>
        <tr>
            <td colspan="2">
                <button type="submit">Save</button>
                <a href="index.php?c=customergroup&a=list">Back</a>
            </td>
        </tr>
    </table>
</form>
